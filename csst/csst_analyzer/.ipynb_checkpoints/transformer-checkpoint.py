import pandas as pd

def average_data_transformer(df: pd.DataFrame, x_col: str, y_col: str):
    """Average one column data as a function of another column
    
    :parameter df: Dataframe with data
    :type df: pd.DataFrame
    :parameter x_col: column in df that y_col is plotted against
    :type x_col: str
    :parameter y_col: column in df to transform into continuous function
    :type y_col: str
    :return:
    :rtype:
    """
    return None
    
